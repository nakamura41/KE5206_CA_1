1. The main files are CNN-VGG16.ipynb and RNN-LSTM.ipynb.
Pure code files without output are CNN-VGG16.py and RNN-LSTM.py.

2. Output of our own CNN model tuning process is in additional file named 'self model output addition.ipynb'

3. The link of our dataset are listed in dataset.txt.

4. The tables being referred by report stores in model_contrast_in_use.xls